# Conexões - IMPORTANTE: escutar em todas interfaces
listen_addresses = '*'
max_connections = 200

# Memória (para 16GB RAM)
shared_buffers = 2GB
work_mem = 64MB
maintenance_work_mem = 512MB
effective_cache_size = 8GB

# WAL
wal_buffers = 64MB
checkpoint_completion_target = 0.9

# Query Planner
random_page_cost = 1.1
effective_io_concurrency = 200
